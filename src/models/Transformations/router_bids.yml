version: 2

models:
  - name: router_bids
    meta:
      experts:
        users:
          - anton@connext.network
    config:
      y42:
        apiVersion: v1
      tags:
        - liquidity
        - dashboard
    columns:
      - name: router_address
        data_type: STRING
      - name: destination_domain
        data_type: STRING
      - name: token_address
        data_type: STRING
      - name: last_bid
        data_type: STRING
