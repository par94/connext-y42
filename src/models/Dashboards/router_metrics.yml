version: 2

models:
  - name: router_metrics
    meta:
      experts:
        users:
          - anton@connext.network
    config:
      y42:
        apiVersion: v1
      tags:
        - dashboard
        - liquidity
